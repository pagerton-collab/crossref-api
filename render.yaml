services:
  - type: web
    name: crossref-api
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    healthCheckPath: "/health"
    envVars:
      - key: DATABASE_URL
        sync: false
